# 业绩基准选择规则

1. 优先使用 `fund_basic.benchmark` 或 `etf_basic.index_code` 明确披露的基准。
2. ETF 跟踪指数必须通过 `etf_basic` 或基金公司公告交叉验证。
3. 用户明确指定基准时保留用户基准，同时披露与产品法定基准的差异。
4. 无明确基准时不得静默使用沪深 300；可以将其作为辅助市场参照并明确标注。
5. 混合型基金的复合业绩基准不能简单替换为单一指数。
6. 基准代码、指数名称、发布机构和数据区间必须进入 Evidence。
